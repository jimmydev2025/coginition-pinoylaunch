export default "\nuniform sampler2D tBuffer;\nuniform sampler2D tBackdrop;\nuniform sampler2D tExtra;\nuniform sampler2D tMask;\nuniform vec2 vBackdropScale;\nvarying vec2 vUv;\nvarying vec2 vMaskUv;\nvarying vec2 vBackUv;\n\nvoid main() {\n    vec3 main = texture2D(tBuffer, vUv).rgb;\n    vec3 extra = texture2D(tExtra, vUv).rgb;\n    vec3 back = texture2D(tBackdrop, vBackUv).rgb * (texture2D(tBackdrop, vBackUv).a);\n    float mask = texture2D(tMask, vMaskUv).r;\n    gl_FragColor = vec4((main * 0.7 * mask + extra + back * 0.7) * 1.4, 1.0);\n}\n";