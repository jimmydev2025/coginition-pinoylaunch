export default "\nuniform sampler2D tBuffer;\nuniform sampler2D tBackdrop;\nvarying vec2 vUv;\nvarying vec2 vUvBack;\n\nvoid main() {\n    vec3 color = texture2D(tBuffer, vUv).rgb;\n    vec3 bg = texture2D(tBackdrop, vUvBack).rgb;\n    float alpha = texture2D(tBuffer, vUv).a;\n    float bgAlpha = texture2D(tBackdrop, vUvBack).a;\n    gl_FragColor = vec4(color * 0.8 * alpha + bg * bgAlpha, 1.0);\n}\n";